# Requirements for Slack Bot with Socket Mode (WebSocket)

# Core dependencies
slack-bolt>=1.14.0
aiohttp>=3.8.0
websockets>=11.0.0
structlog>=22.3.0
prometheus-client>=0.15.0

# Security and validation
cryptography>=3.4.8
validators>=0.20.0

# Optional: For enhanced logging and monitoring
colorlog>=6.7.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-aiohttp>=1.0.4
